name: deploy-prod

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hook this workflow to production ECS deployment with approval gates."
