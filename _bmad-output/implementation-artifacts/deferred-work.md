# Deferred Work

## Deferred from: code review of 9-4-session-expiry-during-open-modal (2026-07-06)

- **`EXPIRES_AT` invalid/unparseable would silently prevent expiry detection forever** (`app/routes/student.py:431`): if the server-rendered `EXPIRES_AT` were ever malformed, `formatRemaining()` would return `'NaN:NaN:NaN'` forever, never the literal `'Expired'` string Story 9.4's guard checks for — the new auto-close/toast logic would simply never fire. Not currently reachable: `expires_at` is always server-generated (`DateTimeHelper.get_future_timestamp(hours=24)`, `app/routes/links.py:89`), never user input.
- **Toast-collision race between the auto-fired expiry toast and `submitAssessment()`'s own toasts** (`app/routes/student.py:738`): `showToast()` has no queue — it just overwrites `textContent`/`className` and reschedules a hide timer. If the expiry toast (8s duration) fires around the same time a submit's own network/error toast fires, whichever was scheduled first can get blanked early by the second, and a candidate could miss reading the expiry notice. Proper fix needs a toast queue; out of scope for a targeted patch.
- **`setInterval(tickTimer, 1000)` keeps firing forever after expiry with no `clearInterval`** (`app/routes/student.py:455`): wasted recomputation with no upper bound once the countdown has reached zero. Matches this codebase's existing accepted pattern of not adding backoff/cleanup to timer loops at current scale.

## Deferred from: code review of 9-2-flag-lifecycle-audit-trail (2026-07-06, finalization review)

- **Uncaught `sqlite3.IntegrityError` on duplicate `event_id`/`override_id`** (`app/services/database_service.py:118`, `flag_submission`/`override_hire_evaluation`): both `flag_events.id` and `score_overrides.id` are `TEXT PRIMARY KEY`; a caller that ever reused a generated id (e.g. a client retry replaying the same UUID) would raise an uncaught `sqlite3.IntegrityError` on the INSERT instead of surfacing the route's intended graceful error response. Extremely low likelihood given UUID generation, and matches this file's existing convention of not catching DB-layer exceptions elsewhere. Fix if it ever becomes a real concern: catch `sqlite3.IntegrityError` around the audit INSERT and return a clear error rather than a raw 500.
- **`hire_row[1]` brittle positional index for `ai_recommendation`** (`app/routes/submissions.py:422`, `override_submission()`): pre-existing pattern from Story 4.4, unrelated to Story 9.2's changes to this route. Fix if `get_hire_evaluation()`'s SELECT column list ever changes: use named tuple/dict access instead of a raw positional index.
- **No index on `flag_events.submission_id`** (`app/models/database.py:174`): every audit-trail read does a full-table scan. Benign at current scale and matches the existing convention — no other audit/history table (`score_overrides`) is indexed either.

## Deferred from: code review of 9-1-batch-dimension-score-queries-in-candidates-endpoint (2026-07-06, finalization review)

- **Unbounded `IN (...)` placeholder count in `get_dimension_scores_for_submissions`** (`app/services/database_service.py:280`): no chunking/batching of the parameter list — a single challenge/assignment whose candidate count exceeds SQLite's host-parameter ceiling (verified empirically at 32766 in this environment's bundled SQLite 3.50.4) would raise an uncaught `sqlite3.OperationalError` and 500 the entire `/candidates` endpoint, a failure mode the old N+1 code could never hit. Not exercised by any test (both test files cap out at 3 candidates). Far outside this story's tested/intended scale (AC1 only specifies correctness at 0/1/50 candidates). Fix if cohort sizes ever realistically approach five figures: chunk `submission_ids` into batches under the parameter ceiling and merge the grouped results.
- **`is_flagged` parity gap between the two candidates endpoints** (`app/routes/assignments.py:94`): `challenges.py`'s candidates payload includes `is_flagged`; the `assignments.py` sibling endpoint (batched in the same story) does not, despite `tests/test_assignments_candidates_endpoint.py`'s own docstring describing the two endpoints as kept in lockstep. Pre-existing gap from a separate party-mode triage item scoped only to `challenges.py`, not introduced by Story 9.1's batching change. Fix: add `s.is_flagged` to `get_candidates_for_assignment`'s SELECT and expose it in the assignments candidate payload, mirroring `challenges.py`.
- **Duplicate `submission_id` rows would inflate the `IN (...)` list unfiltered** (`app/services/database_service.py:280`): `hire_evaluations.submission_id` has no `UNIQUE` constraint, so if `get_candidates_for_challenge`/`get_candidates_for_assignment` ever returned more than one row for the same `submission_id` (e.g. a re-evaluated submission with a stale prior `hire_evaluations` row), `submission_ids = [row[0] for row in rows]` would pass the duplicate straight into the parameter list, needlessly consuming budget. Result would still be correct (dict keys collapse) — a contributing factor to the placeholder-ceiling finding above, not a correctness bug on its own, and contingent on a scenario not currently reachable via the app's actual code paths.

## Resolved 2026-07-04 (post-Story-9.1 code review follow-up)

- **Sibling N+1 pattern in `app/routes/assignments.py:90`** (`GET /api/assignments/<assignment_id>/candidates`): originally deferred out of Story 9.1's scope (see below), then re-raised by the code review of Story 9.1 as a trivial, risk-free reuse of the batched method that was already written and tested. Fixed same-day: `get_candidates()` now collects `submission_ids` up front and calls `db_service.get_dimension_scores_for_submissions()` once instead of once per candidate — identical 3-line change already proven in `challenges.py`. New test file `tests/test_assignments_candidates_endpoint.py` (6 tests — no coverage existed for this endpoint before). Full suite: 101/101 passing.

## Deferred from: Story 9.1 (batch dimension-score queries in candidates endpoint, 2026-07-04)

- ~~**Sibling N+1 pattern in `app/routes/assignments.py:90`**~~ — RESOLVED same-day, see "Resolved 2026-07-04 (post-Story-9.1 code review follow-up)" above. Originally: calls `db_service.get_dimension_scores(submission_id)` once per candidate, the identical pattern Story 9.1 just fixed in `app/routes/challenges.py`'s candidates endpoint. Not part of the original deferred-work.md finding that created Epic 9 (which named only `challenges.py:215`), so left untouched per Story 9.1's explicit scope boundary.

## Resolved 2026-07-04 (bmad-party-mode triage + quick-dev execution)

A party-mode roundtable (John/PM, Winston/Architect, Amelia/Dev) triaged every
item below. 10 quick-dev patches were greenlit and landed same-session, all
covered by new tests (83 passing, up from 64). The original bullets are left
below unedited as the historical record of what was found and by which
story's review — treat the items listed here as CLOSED, not open:

- **Fence-stripping / non-numeric-score / malformed-JSON-shape / prompt-outside-try** (all under "code review of story 7-1..." and "story 7-1..." below) — fixed together as one hardening pass in `evaluation_service.py`: extracted `_parse_dimension_response()` with a single `DimensionParseError`, regex-based fence extraction (handles bare fences and prose-wrapped fences), per-dimension shape/type coercion. Tests: `test_score_8_dimensions.py` (8 new cases).
- **`hire_recommendation`/`composite_score` rounding mismatch** (under "code review of story 7-3...") — composite is now rounded once before classification; storage and classification always agree. Test updated in `test_hire_recommendation_thresholds.py`.
- **Flagged candidates unfiltered in candidates ranking** (under "code review of Epic 4...") — `is_flagged` now added to `get_candidates_for_challenge()` SELECT and the candidate payload (additive only, visibility floor untouched). Test: `test_candidates_endpoint.py`. The re-flagging-overwrite / append-only `flag_events` half of this item is NOT resolved — still open, moved to the hardening-epic backlog.
- **`generate-challenge` returns 200 on silent persist failure** (under "code review of story 7-5...") — added `persisted: bool` to the response.
- **Non-string/null field crash in `generate-challenge`** (under "code review of story 7-5...") — fields now coerced via a type check before `.strip()`; returns a clean 400.
- **No whitelist on `ai_assistance_mode`** (under "code review of 6-5...") — `links.py` now validates against `Config.VALID_ASSISTANCE_MODES`, logging a warning and falling back to the default on drift instead of silently going unrestricted.
- **Magic string `'unguarded'` duplicated** (under "code review of 6-5...") — extracted to `Config.DEFAULT_ASSISTANCE_MODE`, used by both `links.py` and `docker_service.py`.
- **Dead `_run()` call in `get_file_from_container`** (under "code review of story 1-4...") — removed; the single remaining call already ran in the correct binary mode.
- **`flask run` produces no INFO logging** (under "code review of story 1-4...") — `create_app()` now calls `logging.basicConfig()` guarded by `if not logging.root.handlers`.
- **Five `student.py` JS issues** (`submitBtn` stuck disabled, `escHtml` not escaping quotes, no re-entrancy guard on `startPolling`, permanent HTTP errors treated as transient, no Escape-key modal handler — spread across the 6.2/6.3/6.4 review entries below) — all fixed in one pass. No JS test harness exists in this repo; verified by starting the real dev server, generating a real candidate link, fetching the rendered page, confirming all five fixes are present in the served HTML, and syntax-checking the extracted script with `node --check`.

**Explicitly NOT touched** (settled/accepted v1 scope, reconfirmed during this triage — see AGENT.md "Next Session" section for the full reasoning): guarded-mode honor-system bypassability, no-auth-by-design surfaces (`flagged_by`, `/student/preview`), concurrent-override double-counting, and everything else the original reviews called benign-at-scale or convention-matching (workspace param in `extract_container_files`, cap-accounting quirks, missing try/except on `get_challenge()`, AC3 wording, chmod 666, test-import coupling, CSS duplication, no poll backoff/jitter, background thread not catching `BaseException`).

**Still open — now scoped as Epic 9 (Production Hardening)** in `sprint-status.yaml`, stories 9-1 through 9-6, all `backlog`, not urgent: N+1 candidates query, `flag_events` append-only table + re-flagging-overwrite fix, guarded-mode injection-failure visibility on the dashboard, session-expiry-during-open-modal handling, assignment/link-keyed preview fidelity (blocked on a UI-placement decision), Gemini CLI interactive-picker manual QA pass.

## Deferred from: 8-1-pre-authenticate-gemini-cli-in-student-container (2026-07-04)

- **Interactive-picker fix not manually confirmed in a real browser terminal** (`docker/Dockerfile.codeserver`): The `security.auth.selectedType: "gemini-api-key"` fix is sourced directly from the installed CLI's own bundled, version-matched docs and doesn't regress headless mode (both verified live). But actually watching the interactive auth picker disappear inside a real terminal couldn't be automated in this session — `docker exec -t` without a genuinely attached terminal doesn't reliably drive Gemini CLI's ink-based interactive UI (attempts hung). Recommended: one manual pass — generate a candidate link, open the code-server terminal in an actual browser, type `gemini`, confirm no auth-method picker appears before fully closing this out.

## Deferred from: code review of 6-5-guarded-mode-claude-restrictions (2026-07-03)

- **Guarded-mode injection failure is silent** (`app/services/docker_service.py`, `inject_workspace_files()`): found during the re-review after the chmod-skip patch. If the `GEMINI.md` write itself fails (transient Docker/disk issue — distinct from a candidate deliberately deleting the file post-injection), the function logs a debug-level warning and link generation proceeds normally. No employer or candidate is told the assessment silently ran unguarded. Future story: surface `ai_assistance_mode` and injection success/failure on the teacher dashboard / submission record so a failed guarded-mode injection is visible after the fact.

- **Guarded mode is enforcement-by-honor-system, trivially bypassable** (`app/services/docker_service.py`, `student_preview`... n/a — `inject_workspace_files()`): The entire mechanism is a `GEMINI.md` file politely asking the in-container Gemini CLI to restrict itself. The candidate has a full shell in the container and `/workspace` is world-writable (`drwxrwxrwx`, confirmed live) — they can delete/edit the file, invoke `gemini` from a different working directory so it never loads, or use an AI tool entirely outside the sandbox. Decided during code review (2026-07-03, user choice): ship as-is for v1 as a soft nudge, not a hard guardrail. Real backend enforcement (e.g. proxying/validating Gemini API calls from the container, an immutable bind-mount for GEMINI.md) is a future story if assessment integrity requirements demand it. (Provider migrated from Claude Code CLI to Gemini CLI 2026-07-04 — see AGENT.md Phase 2; this constraint applies unchanged to the new provider.)
- **No whitelist/validation on `ai_assistance_mode` string match** (`app/routes/links.py:29`): Comparison is an exact string match against `'guarded'`; any drift (typo, case, whitespace from a future manual DB edit or code change) silently falls back to unrestricted with no warning. Low practical risk today since the value is enum-validated at challenge creation (`POST /api/generate-challenge` / `/api/challenges` reject anything outside `{guarded, unguarded}`), and hardening the string match wouldn't meaningfully improve security anyway given the honor-system limitation above.
- **Magic string `'unguarded'` duplicated as a default value** (`app/routes/links.py:26` and `app/services/docker_service.py:107`): Two independent hardcoded literals that must stay in sync by convention. Future story: extract to a shared constant if a third mode is ever added.
- **No try/except around the new `db_service.get_challenge()` call** (`app/routes/links.py:30`): If it raises (DB lock/connection issue), the whole `generate_link` request fails. Matches the existing convention two lines above (`get_assignment()` is equally unguarded) — not a new fragility introduced by this story, just extending the existing pattern.
- **AC3's "(deleted/soft-deleted challenge)" example is currently unreachable**: `get_challenge()` fetches "regardless of publish status" (its own docstring) and the app has no hard-delete path for challenges — `DELETE /api/challenges/<id>` only sets `is_published = -1`, the row and its `ai_assistance_mode` persist forever. So a soft-deleted guarded challenge keeps enforcing guarded mode on any assignment still referencing it — arguably the *correct* behavior (an already-created assignment shouldn't silently lose its restriction because the catalog entry was unpublished), but it means AC3's stated fallback path never actually triggers in practice. Spec-wording note for a future story-file cleanup, not a code defect — the code correctly implements AC3's literal text (falls back to `'unguarded'` only when `get_challenge()` returns `None`).
- **`chmod 666` on the injected `GEMINI.md`** (`app/services/docker_service.py`, `inject_workspace_files()`): Matches the existing permission treatment of `instructions.md`/`solution.py`, but means the candidate has write access to their own restriction file — component of the honor-system decision above, not independently worth a different chmod value (since `/workspace` itself is world-writable, restricting the file alone wouldn't prevent deletion).

## Deferred from: code review of 6-4-preview-as-student (2026-07-03)

- **No auth/role check on `/student/preview/<challenge_id>`** (`app/routes/student.py:647`): Any caller who knows or guesses a `challenge_id` can view its title, description, rubric, and starter code, published or not. Deferred — matches the app-wide no-auth dev posture documented in CLAUDE.md; becomes a real issue when auth is added.

- **Permanent HTTP errors (404/500) during polling treated as transient** (`app/routes/student.py:614`, `startPolling`): The `catch` block treats any fetch failure — network hiccup or permanent server error — the same way, silently retrying until the 60s timeout. A genuine backend error surfaces to the candidate as "taking longer than expected" instead of an accurate failure state. Future story: distinguish permanent (4xx/5xx) from transient errors and show a distinct error state.

- **No re-entrancy guard on `startPolling`** (`app/routes/student.py:605`): Nothing prevents a double-click or duplicate submit event from invoking `startPolling` twice concurrently, spinning up two overlapping polling loops hitting `/api/submission/<id>` in parallel. Low real-world impact given the confirm-modal step already reduces double-submit likelihood.

- **CSS duplication between `student_dashboard()` and `student_preview()` templates** (`app/routes/student.py`): Gradient headers, card styling, topbar, and buttons are hand-duplicated across both f-string templates rather than shared. Any future visual fix to the real assessment UI must be manually re-applied to the preview template or the two will drift apart.

- **Fixed poll interval/timeout, no backoff or jitter** (`app/routes/student.py:609-610`): `POLL_MS = 3000`, `TIMEOUT = 60000` are hardcoded with no jitter — every submitting candidate polls on the same fixed cadence, a needless synchronized load pattern if many candidates submit around the same time. Not a concern at current scale.

- **`escHtml` doesn't escape single quotes, unlike Python-side `html.escape`** (`app/routes/student.py:558`): Two inconsistent, hand-maintained escaping implementations for the same "render untrusted text safely" concern. Low risk today since `escHtml`'s only call site renders into a `<p>` element body, not an attribute value, but invites a future call site to assume more coverage than exists.

- **Assignment/link-keyed preview (true candidate fidelity)** (new story, unscheduled): `student_preview()` reads from the `challenges` catalog template, not the live `assignments` row a candidate actually sees — these can diverge once an employer edits an assignment post-generation. Resolved via BMad party-mode panel (John/Winston/Amelia/Sally, unanimous 2026-07-03): no frontend button exists yet for the current route, so no code change was forced now; AC3 was reworded to describe template-preview accurately instead. Follow-up story needed once UI placement is decided (assignment-detail-page trigger vs. catalog trigger) — that decision determines whether `challenge_id` uniquely resolves to one assignment (it does not; a challenge can spawn multiple assignments) and whether new disambiguation logic is required.

## Deferred from: code review of 6-3-real-polling-instead-of-fixed-2s-wait (2026-07-03)

- **`submitBtn` stays disabled after network error** (`app/routes/student.py` ~line 508): Pre-existing issue from story 6.2 — `submitBtn.disabled = true` is set before the try block and never reset in `finally`. After a network error the student cannot retry without a hard-refresh. Already tracked in deferred-work from story 6.2.

## Deferred from: code review of 6-2-verification-nudge-before-submission (2026-07-02)

- **`submitBtn` stays disabled after failed submission** (`app/routes/student.py`): `submitAssessment()` sets `submitBtn.disabled = true` and only re-enables `confirmBtn` in `finally`. On network error the student must hard-refresh to retry. Fix: add `submitBtn.disabled = false` to the `finally` block alongside `confirmBtn`. Pre-existing before this story.

- **Session expiry during modal leaves no warning** (`app/routes/student.py`): If the session timer hits zero while `#nudgeModal` or `#submitModal` is open, nothing closes the overlay or alerts the student. When they proceed, the API rejects the expired session silently. Future story: hook the timer to auto-close open modals and display an expiry toast.

- **No Escape key handler on any modal** (`app/routes/student.py`): Keyboard-only users cannot dismiss modals with Escape — only visible buttons work. All modals in this file share this limitation. Future story: add a global `keydown` listener that calls the close function for whichever modal has `.show`.


## Deferred from: code review of Epic 4 stories 4.1–4.5 (2026-07-02)

- **N+1 DB queries in candidates endpoint** (`app/routes/challenges.py:215`): One `get_dimension_scores()` query per candidate, unbounded. For large cohorts (100+ candidates) this will be slow. Fix: batch-load all dimension scores in one `WHERE submission_id IN (...)` query before building the candidates list. Deferred because Story 4.2 spec accepted this pattern and no pagination AC exists.

- **Flagged candidates unfiltered in candidates ranking** (`app/services/database_service.py` — `get_candidates_for_challenge` query): Flagged submissions appear in the ranked candidate list with no `is_flagged` indicator in the payload. Recruiters cannot distinguish flagged-for-review candidates from clean ones. Future story: add `s.is_flagged` to `get_candidates_for_challenge` SELECT and expose in candidate object.

- **Re-flagging overwrites prior flag metadata** (`app/routes/submissions.py:361`, `app/services/database_service.py`): Second `POST /submissions/<id>/flag` call overwrites `flag_by`, `flag_reason`, `flagged_at` with no history. Unlike overrides (which have `score_overrides` append-only log), flags have no audit trail. Future story: add `flag_events` append-only table; or add `WHERE is_flagged = 0` guard to prevent overwrite.

- **`flagged_by` is caller-supplied with no authentication** (`app/routes/submissions.py:360`): Any caller can claim any identity in `flagged_by`. Deferred — system has no auth by design (CLAUDE.md). Becomes a real issue when auth is added.

- **Concurrent overrides double-count in analytics** (`app/routes/submissions.py:371–410`): Two simultaneous POST requests to `/submissions/<id>/override` both pass the existence check and both insert into `score_overrides`, double-counting one override event. Deferred — single-tenant dev-phase system; no concurrency requirement exists.

## Deferred from: code review of story 1-4-replace-print-with-proper-logging (2026-07-02)

- **Dead `_run()` call in `get_file_from_container`** (`app/services/docker_service.py:73`): First `_run(['cp', container_id:path, '-'])` at line 73 uses `text=True` which causes `UnicodeDecodeError` on binary tar content; the working binary-mode `subprocess.run()` at line 79 is unreachable for plain files. For ASCII Python files the extra call is merely wasteful; for non-UTF-8 content the method silently returns `None`. Fix: remove the dead `_run()` call at line 73–74; the binary-mode call at line 75–79 is the one that actually works.

- **`flask run` entry point produces no INFO-level logging** (`run.py`): `logging.basicConfig()` is only configured in `run.py`. If a developer starts the server with `flask run` (which calls `create_app()` directly), the root logger defaults to WARNING level and all `logger.info()` / `logger.debug()` calls (evaluation completion, container start, file reads) are silently dropped. Fix: add a guard-style `logging.basicConfig()` call in `create_app()` — e.g., `if not logging.root.handlers: logging.basicConfig(...)` — without violating AC4's spirit of not having unconditional logging config inside `app/`.

- **Background evaluation thread does not catch `BaseException`** (`app/routes/submissions.py:17`, `evaluate_submission_files`): The outer `except Exception as e:` block does not catch `BaseException` subclasses (e.g., `MemoryError`, signal delivery). If such an exception escapes the LLM call, the thread dies silently, `update_submission_evaluation()` is never called, and the submission row stays permanently pending (NULL score/feedback) with no log entry. Deferred — very rare failure mode; no timeout/watchdog story exists yet.

## Deferred from: story 7-1-unit-test-score_8_dimensions (2026-07-03)

- **Non-numeric dimension score crashes `score_8_dimensions`** (`app/services/evaluation_service.py:217-220`): A syntactically valid JSON response with a non-numeric `"score"` (e.g. `"score": "high"`) raises an uncaught `TypeError` at the composite `sum()` — the weighted-average block sits OUTSIDE the try/except that guards parsing. The background evaluation thread's outer handler catches it, but the submission gets no safe-default result. Fix: coerce/validate `dims[d]["score"]` to a number (default 0) during the backfill loop at line 211, then add a unit test for it.

## Deferred from: code review of story 7-1-unit-test-score_8_dimensions (2026-07-03)

- **Fence stripping only handles the exact ```json prefix — other valid LLM outputs silently zero-score candidates** (`app/services/evaluation_service.py:200-201`): A bare ``` fence (no `json` tag) makes `split("```json")[-1].split("```")[0]` return `""`, and prose before the fence defeats the `startswith("```")` check entirely; both paths fall into the safe default (composite 0, "pass") even though valid JSON was present. HIGH product impact: a real candidate gets zero-scored by a formatting quirk. Fix: robust extraction (regex for first `{...}` block or fence-agnostic strip) + unit tests for bare-fence and prose-wrapped variants.

- **Prompt-building runs outside the try/except in `score_8_dimensions`** (`app/services/evaluation_service.py:92-183`): `assignment=None`, a non-dict entry in `session_logs`, or a non-dict `file_snapshot` raises before the safety net at line 196, contradicting the docstring promise of a safe default on any failure. Fix: widen the try block to cover formatting, or validate inputs up front.

- **Malformed-but-valid-JSON response shapes crash after the safety net** (`app/services/evaluation_service.py:210-220`): Top-level JSON that is not a dict (`"[]"`, `"null"`, `"42"`) raises AttributeError at `result.get`; `"dimensions"` as a non-dict raises TypeError at backfill; a dim entry missing `"score"` raises KeyError in the composite sum. Siblings of the already-logged non-numeric-score TypeError. Fix together: validate/coerce the parsed shape (dict check, per-dim `{"score": number}` coercion with 0 default) then add unit tests for each shape.

- **Tests transitively import all app.routes modules** (`app/__init__.py:14-19`): Any `from app.services...` import executes the package `__init__`, which imports all seven route blueprints (and `load_dotenv(override=True)`). Test isolation from the routes/Flask layer is unachievable without restructuring package imports (e.g., moving blueprint imports inside `create_app()`). Low priority; matters if route modules ever gain import-time side effects beyond `DatabaseService()` instantiation.

## Deferred from: code review of story 7-2-unit-test-extract_container_files (2026-07-03)

- **`workspace` parameter ignored by path normalization in `extract_container_files`** (`app/services/evaluation_service.py:67`): Key normalization hardcodes `replace('workspace/', '', 1)` regardless of the `workspace` argument. Called with e.g. `workspace='/data'`, keys keep a `data/` prefix; and because `replace` matches anywhere, a member `data/workspace/x.py` would lose the wrong segment. Latent — all current callers use the default `/workspace`. Fix: derive the strip prefix from the `workspace` argument (`workspace.strip('/') + '/'`).

- **Cap accounting quirks in `extract_container_files`** (`app/services/evaluation_service.py:68-74`): (1) the 50KB budget counts raw tar bytes but the stored value is decoded text — invalid bytes each become U+FFFD (3 bytes re-encoded), so text forwarded to the LLM can exceed the cap when snapshots contain binary-ish content; (2) the `\n[TRUNCATED]` marker rides above the cap (+12 chars per truncated file); (3) duplicate tar member names overwrite last-wins while double-counting `total_bytes`. All benign at current scale; fix together if the cap ever becomes a hard token-budget guarantee.

## Deferred from: code review of story 7-5-unit-test-generate_challenge-with-new-params (2026-07-03)

- **`POST /api/generate-challenge` returns 200 even when persistence silently fails** (`app/routes/challenges.py:100-103`): if `db_service.create_challenge(...)` raises (disk full, lock contention), the route logs a warning and still returns 200 with `challenge_id: null` — the client has no field distinguishing "saved" from "generated but lost". This is intentional graceful-degradation from Story 3.4 (generation succeeding is what matters), not a bug, but worth reconsidering — e.g. add a `persisted: bool` field to the response so callers can detect and retry/warn on data loss.

- **Non-string/null field values crash with an unhandled `AttributeError`, not a clean 400** (`app/routes/challenges.py:52-56`): `data.get('difficulty', '').strip()` (and the same pattern for the other fields) assumes the JSON value is always a string. `{"difficulty": null}` makes `.get()` return `None`, and `None.strip()` raises `AttributeError` — this happens BEFORE the route's own try/except (which only wraps the `EvaluationService.generate_challenge()` call), so nothing in the route catches it. Confirmed empirically via the Flask test client (Story 7.5): under `TESTING=True` the exception propagates and is re-raised rather than converted to a 500 response — in a real (non-testing) deployment this would surface as Flask's generic unhandled-exception 500 page. Same risk for `challenge_type`, `skill_area`, `ai_assistance_mode`, `problem_statement`. Fix: coerce non-string values to `''` before `.strip()`, or validate `isinstance(x, str)` up front.

## Deferred from: code review of story 7-3-unit-test-hire-recommendation-thresholds (2026-07-03)

- **`hire_recommendation` and `composite_score` can visibly contradict each other near a threshold** (`app/services/evaluation_service.py:224-233`): the if-chain classifies against the pre-round `composite` (line 224), but `composite_score` is stored post-round (`round(composite, 2)`, line 233). Confirmed empirically: a composite of `84.996` evaluates `>= 85` as `False` (→ `"hire"`) while `round(84.996, 2) == 85.0` is stored — a recruiter sees `85.0` labeled `hire`, one point short of `strong_hire`, with no visible reason. Fix: round once, before the if-chain (`composite = round(min(100.0, max(0.0, composite)), 2)`), then branch and store the same rounded value.

## Deferred from: code review of story 9-3-surface-guarded-mode-injection-failures (2026-07-05)

- **`inject_workspace_files()`'s `injected` flag is computed but never persisted or surfaced** (`app/services/docker_service.py:183`): the return dict distinguishes a total injection failure (`instructions.md`/`solution.py` never written — a rare/fatal container issue) from a narrower guarded-mode-only `GEMINI.md` failure, but only `guarded_mode_enforced` made it onto `session_links`/the submission API in Story 9.3. A total failure currently leaves no audit trail at all. Out of Story 9.3's guarded-mode-specific scope; a future story could extend `create_session_link`/`get_session_link_assistance_info` to also persist `injected`.

- **`/api/challenges/<id>/candidates` doesn't expose `guarded_mode_enforced`** (`app/routes/challenges.py`): Story 9.3 only surfaced the field on the single-submission `GET /api/submission/<id_or_link>` endpoint. An employer scanning a batch of candidates has no way to spot an unguarded-injection failure without opening each submission individually. Fix: include `guarded_mode_enforced` in the per-candidate dict built by the candidates route (same pattern as the `is_flagged` field added in Story 9.1's N+1 fix).
