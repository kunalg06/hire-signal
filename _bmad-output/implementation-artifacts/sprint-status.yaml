# generated: 2026-07-02
# last_updated: 2026-07-02
# project: coding_platforms
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: "2026-07-02"
last_updated: "2026-07-06"  # updated: Stories 9-1, 9-2, 9-4, 9-5, 9-7 all done (132/132 suite green). 9-7's code review found and fixed a critical directory-mount gap same-day, and surfaced an unfixable HOME-env-var bypass accepted as a documented residual gap. 9-6 still backlog (manual QA, needs Docker; now also covers live-verifying 9-7's bind mount).
project: coding_platforms
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 1 — Bug Fixes & Foundation Hardening
  # All stories done as of 2026-07-02.
  # ─────────────────────────────────────────────────────────────────────────
  epic-1: done

  1-1-fix-efficiency-score-bug: done
  # Fixed 2026-06-30: submissions.py now queries submissions table for link_id,
  # then passes to db_service.get_link_created_time()

  1-2-pass-container_id-into-evaluation-pipeline: done
  # Fixed 2026-06-30: container_id flows from submit_with_files() into
  # evaluate_submission_files() and EvaluationService.evaluate_code()

  1-3-score-bounds-enforcement: done
  # Fixed 2026-06-30: min(100.0, max(0.0, combined_score)) applied in
  # evaluation_service.py

  1-4-replace-print-with-proper-logging: done
  # Story file created 2026-07-02; 33 print() calls across 8 files, mechanical substitution

  epic-1-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 2 — 8-Dimension AI Scoring Engine
  # All 6 stories complete as of 2026-07-01.
  # ─────────────────────────────────────────────────────────────────────────
  epic-2: done

  2-1-schema-dimension-scoring-tables: done
  # dimension_scores + hire_evaluations tables in app/models/database.py

  2-2-full-workspace-snapshot-at-submission-time: done
  # EvaluationService.extract_container_files() — Docker tar archive,
  # 50KB cap, text-only filter, {} fallback

  2-3-8-dimension-claude-scoring-prompt: done
  # EvaluationService.score_8_dimensions() — single Claude call,
  # all 8 keys guaranteed even on parse failure

  2-4-hire-recommendation-logic-python-enforced: done
  # Python thresholds: strong_hire>=85, hire>=70, select>=55, pass<55
  # Claude's threshold recommendation logged but not used

  2-5-persist-dimension-scores-to-db: done
  # Per-dimension rows to dimension_scores; verdict to hire_evaluations
  # with dimension_weights_json snapshot for auditability

  2-6-updated-submission-api-response: done
  # GET /api/submission/<id> returns dimensions dict (8 keys) and
  # hire_evaluation (composite_score, recommendation, rationale, weights)

  epic-2-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 3 — Market-Aligned Challenge System
  # All 6 stories complete as of 2026-07-02.
  # ─────────────────────────────────────────────────────────────────────────
  epic-3: done

  3-1-schema-challenges-catalog-table: done
  # challenges table added to app/models/database.py — 12 columns, idempotent

  3-2-extended-challenge-generation-api: done
  # POST /api/generate-challenge accepts challenge_type, skill_area,
  # ai_assistance_mode with enum validation (400 on invalid)

  3-3-richer-challenge-generation-prompt: done
  # generate_challenge() rewritten — market-aligned prompt (Stripe/Anthropic
  # style), per-type scaffolding, per-skill-area imports, mode-aware instructions

  3-4-auto-persist-generated-challenge-to-catalog: done
  # Generated challenges saved as is_published=0; challenge_id in response

  3-5-challenge-catalog-endpoints: done
  # GET /api/challenges, GET /api/challenges/<id>,
  # POST /api/challenges/<id>/publish, DELETE /api/challenges/<id>,
  # GET /api/challenges/meta/options

  3-6-seed-10-curated-challenges: done
  # Implemented 2026-07-02; scripts/seed_challenges.py seeding 10 challenges across all types/areas
  # Review patches 2026-07-02: removed init_db() violation, dead import, added per-challenge error handling

  epic-3-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 4 — Candidate Comparison & Hiring Workflow
  # Not started. Deferred to Phase 2.
  # ─────────────────────────────────────────────────────────────────────────
  epic-4: in-progress

  4-1-schema-comparison-sessions: done
  # comparison_sessions + score_overrides tables in database.py; 3 comparison session service methods

  4-2-candidate-comparison-endpoint: done
  # GET /api/challenges/<challenge_id>/candidates — sort_by/order, dim_averages, rank; dim_averages filter fixed (review patch)

  4-3-human-override-flag-for-manual-review: done
  # POST /flag + /override endpoints; link_id fallback query flag fields fixed (review patch)

  4-4-override-logging-as-calibration-dataset: done
  # score_overrides table + GET /api/analytics/overrides + frontend sync fixes; return-value guard added (review patch)

  4-5-visibility-floor-enforcement: done
  # sort_key math.inf sentinel + is_evaluated field; migration exception narrowed (review patch)

  epic-4-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 5 — Employer Dashboard UI Overhaul
  # Stories 5.1, 5.2, 5.4, 5.5 complete (done in Phase 1 frontend pass).
  # Story 5.3 (butterfly chart / radar overlay) deferred to Phase 2.
  # ─────────────────────────────────────────────────────────────────────────
  epic-5: done

  5-1-challenge-results-board-candidate-grid: done
  # Tab 4 in frontend.html — assignment dropdown, candidate grid,
  # filter buttons (All/Strong Hire/Hire/Select/Pass), sort dropdown,
  # detail panel with radar + dimension breakdown

  5-2-8-dimension-radar-chart-svg-component: done
  # 8-spoke SVG radar — abbreviated labels (PD/FP/CP/IQ/DA/AD/CC/TE),
  # mini (80px) in grid, full (300px) in detail panel

  5-3-side-by-side-candidate-comparison-view: done
  # Story created 2026-07-02; frontend-only (templates/frontend.html), no backend changes
  # Overlaid radar + butterfly chart + expandable rationale + cross-assignment warning modal
  # Implemented + code review complete 2026-07-02; 4 major issues fixed

  5-4-hire-recommendation-surface: done
  # Hire badge (green/blue/amber/gray), AI narrative paragraph,
  # composite score displayed — flag/override actions depend on Epic 4

  5-5-ai-beta-transparency-banner: done
  # "AI scoring is experimental" banner in frontend.html,
  # dismissible per session via localStorage

  epic-5-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 6 — Student Experience & Preview as Student
  # Story 6.1 complete (workspace injection uses three-panel format).
  # Stories 6.2–6.5 deferred to Phase 2.
  # ─────────────────────────────────────────────────────────────────────────
  epic-6: done

  6-1-structured-challenge-display-in-student-view: done
  # instructions.md injected with Scenario / Your Task / Evaluation Criteria
  # three-panel format via DockerService.inject_workspace_files()

  6-2-verification-nudge-before-submission: done
  # Implemented + code review complete 2026-07-02; wording trimmed to exact spec; 3 pre-existing items deferred

  6-3-real-polling-instead-of-fixed-2s-wait: done
  # Implemented 2026-07-03; startPolling() + showSubmittedResults/Polling/Timeout in student.py; 3s interval / 60s timeout
  # Code review 2026-07-03: composite_score falsy-fallback fixed (0-score edge case)

  6-4-preview-as-student: done
  # GET /student/preview/<challenge_id> — preview mode route added to student.py; all 7 ACs satisfied
  # Code review 2026-07-03: fixed hire_data/hire_evaluation key mismatch (polling never completed),
  # unescaped rec label, NaN score guard, missing-submissionId guard. AC3 reworded via party-mode
  # panel decision (challenge-template preview, not assignment-fidelity); 7 items deferred.

  6-5-guarded-mode-claude-restrictions: done
  # Implemented 2026-07-03: inject_workspace_files() writes /workspace/CLAUDE.md for guarded
  # challenges; links.py resolves ai_assistance_mode via challenge_id (index 6, verified live).
  # Smoke-tested end-to-end through real Docker containers via the Flask route.
  # Code review 2026-07-03: chmod-skip-on-CLAUDE.md-failure bug fixed (verified live);
  # guarded mode confirmed honor-system-only, accepted as v1 scope, 5 items deferred.

  epic-6-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 7 — Test Coverage
  # Done as of 2026-07-03 — all 5 stories (7.1-7.5) implemented and code
  # reviewed. 64 tests total across 4 new test files + root conftest.py.
  # This completes the entire sprint (Epics 1-7 all done).
  # ─────────────────────────────────────────────────────────────────────────
  epic-7: done

  7-1-unit-test-score_8_dimensions: done
  # Implemented + code review complete 2026-07-03: tests/test_score_8_dimensions.py
  # (12 tests after 8 review patches, all green, hermetic) + root conftest.py.
  # 4 production gaps deferred (fence-stripping zero-score risk is the notable one)

  7-2-unit-test-extract_container_files: done
  # Implemented + code review complete 2026-07-03: tests/test_extract_container_files.py
  # (16 tests after 6 review patches, 28 total suite green). 2 production gaps deferred
  # (workspace-param path normalization; cap byte/char accounting quirks)
  # Story file created 2026-07-03; mock DockerService.get_archive, in-memory tar
  # fixtures; b''/raise → {}, text-only filter, 50KB cap + [TRUNCATED] marker

  7-3-unit-test-hire-recommendation-thresholds: done
  # Implemented + code review complete 2026-07-03: tests/test_hire_recommendation_thresholds.py
  # (10 tests after 3 review patches, 38 total suite green). 1 production gap deferred:
  # composite_score/hire_recommendation can visibly disagree near a boundary (round-after-compare)

  7-4-integration-test-get-api-challenges-id-candidates: done
  # Implemented + code review complete 2026-07-03: tests/test_candidates_endpoint.py
  # (13 tests after 11 review patches, 51 total suite green). Review caught and fixed
  # a real test-isolation gap: create_app() with no config touched the real DB via
  # its own internal init_db() call — fixed via create_app("testing"). Zero production
  # gaps found (first Epic 7 story with no deferred-work.md entry).

  7-5-unit-test-generate_challenge-with-new-params: done
  # Implemented + code review complete 2026-07-03: tests/test_generate_challenge_endpoint.py
  # (13 tests after 10 review patches, 64 total suite green). Reused 7.4's DB-isolation
  # fixtures + 7.1's LLM-mock pattern. Fixed a vacuous "nothing persisted" assertion caught
  # independently by all 3 review layers. Found + deferred a genuine production gap: non-string
  # field values crash with unhandled AttributeError instead of clean 400.

  epic-7-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 8 — Gemini Provider Follow-Up (post Phase 2 migration)
  # Story 8.1 done as of 2026-07-04.
  # ─────────────────────────────────────────────────────────────────────────
  epic-8: in-progress

  8-1-pre-authenticate-gemini-cli-in-student-container: done
  # Implemented + verified 2026-07-04: found that interactive `gemini` (the
  # real candidate terminal path) shows an auth-method picker on first launch
  # even with GEMINI_API_KEY set, unlike headless mode which was already fine.
  # Fixed via security.auth.selectedType="gemini-api-key" baked into
  # ~/.gemini/settings.json in docker/Dockerfile.codeserver. Also independently
  # re-verified guarded/unguarded mode works correctly on Gemini CLI post-migration
  # (GEMINI.md present -> declines full solutions; absent -> gives full solutions).

  8-2-fix-gemini-json-response-reliability: done
  # Implemented + verified 2026-07-04: live user hit a JSON-parse error on
  # POST /api/generate-challenge. Root-caused to two bugs: (1) thinking tokens
  # eating the max_output_tokens budget (fixed via thinking_budget=0), and
  # (2) response_mime_type alone unreliable for multi-line starter_code strings
  # (fixed via response_schema — true structured-output constrained decoding).
  # LLMService.chat() gained an optional response_schema param; both
  # generate_challenge() and score_8_dimensions() now pass their schema.
  # Verified: 22 live calls, 0 failures, vs. a reproducible pre-fix failure.

  8-3-fix-ide-install-code-cli-not-found: done
  # Implemented + verified 2026-07-04: live user hit "VS Code CLI not found"
  # running /ide install inside the real code-server terminal (while confirming
  # Story 8.1's auth fix). Root-caused via installed CLI's bundled source:
  # /ide install hardcodes a search for a `code` binary; code-server only ships
  # `code-server`. Fixed with a `code -> code-server` symlink in
  # docker/Dockerfile.codeserver (code-server supports the same
  # --install-extension/--force flags). Verified: extension installs
  # successfully as both root and the real runtime user `coder`; no regression
  # to headless gemini or the test suite.

  8-4-retry-on-gemini-json-parse-failure: done
  # Implemented + verified 2026-07-04: live user hit the EXACT SAME error as
  # Story 8.2 a second time, after that fix was already deployed. Ruled out
  # stale-server-process and reverted-fix explanations (curl'd the live
  # server directly - it had the fix and that call succeeded; fresh-process
  # stress tests also passed). Concluded response_schema reduces but doesn't
  # eliminate the failure rate. Fixed via EvaluationService._call_llm_for_json(),
  # a shared retry helper (max 3 attempts) used by both generate_challenge()
  # and score_8_dimensions() - retries ONLY on JSON-parse/validation failure,
  # never on genuine LLMService.chat() exceptions (preserves the existing
  # exact-call-count test for provider-down errors). Verified via a mocked
  # 2-fail-then-succeed recovery test + live stress runs; full suite unchanged
  # at 64/64.

  epic-8-retrospective: optional

  # ─────────────────────────────────────────────────────────────────────────
  # Epic 9 — Production Hardening (deferred-work backlog)
  # Scoped 2026-07-04 via bmad-party-mode triage (John/PM, Winston/Architect,
  # Amelia/Dev) of every item in deferred-work.md. Not started — none of the
  # 10 quick-dev items from that same triage belong here (those landed the
  # same session as direct patches, see deferred-work.md's "Resolved
  # 2026-07-04" section). This epic is specifically the items the panel
  # judged too structural for a quick-dev patch: schema/behavior decisions,
  # a batched-query rewrite needing a regression test first, and one item
  # blocked on a product decision. Not urgent — pick up when capacity allows.
  # ─────────────────────────────────────────────────────────────────────────
  epic-9: in-progress

  9-1-batch-dimension-score-queries-in-candidates-endpoint: done
  # Implemented 2026-07-04: added DatabaseService.get_dimension_scores_for_submissions()
  # (one batched query, grouped by submission_id) and rewired get_challenge_candidates()
  # to call it once instead of once per candidate. Sort/rank/dimension_averages/is_flagged
  # logic untouched. 17/17 tests in test_candidates_endpoint.py pass (14 pre-existing
  # unmodified + 3 new), 95/95 full suite green. Sibling N+1 in assignments.py:90 found,
  # confirmed out of scope per story, logged to deferred-work.md.
  # app/routes/challenges.py:215 issues one get_dimension_scores() query per
  # candidate (N+1), flagged since Story 4.2 but accepted then (no pagination
  # AC existed). Fix: batch into one WHERE submission_id IN (...) query.
  # Winston's condition (2026-07-04 triage): must ship WITH a regression test
  # proving the visibility floor (un-evaluated candidates sort last via the
  # math.inf/-math.inf sentinel) survives the rewrite — a LEFT JOIN returning
  # NULL rows instead of no rows could silently break that guarantee as a
  # side effect nobody intended.
  # Finalization code review 2026-07-06 (3-layer: Blind Hunter, Edge Case Hunter,
  # Acceptance Auditor): 0 decision-needed, 0 patch, 3 defer (unbounded IN(...)
  # placeholder ceiling at extreme cohort sizes, is_flagged parity gap vs.
  # assignments.py, duplicate-submission_id edge case), all logged to
  # deferred-work.md. Acceptance Auditor verified all 5 ACs against current
  # source + a live 23/23 test run. Marked done.

  9-2-flag-lifecycle-audit-trail: done
  # Implemented 2026-07-04: added flag_events append-only table (mirrors
  # score_overrides exactly) and DatabaseService.log_flag_event(), wired
  # into flag_submission() the same way override_submission() calls
  # log_score_override(). Re-flagging now preserves full history instead
  # of overwriting it; submissions.flag_reason/flag_by/flagged_at still
  # show only the latest flag (unchanged, verified by a dedicated test).
  # No new endpoint added (out of scope per AC6). 104/104 full suite green
  # (101 before this story + 3 new).
  # Finalization code review 2026-07-06 (3-layer: Blind Hunter, Edge Case
  # Hunter, Acceptance Auditor): all 3 layers independently found the
  # "unconditional" audit write from the 2026-07-04 follow-up was still
  # caller-opt-in (event_id/override_id defaulted to None with a silent
  # skip) — tests/test_candidates_endpoint.py:286 was already exercising
  # the gap live. 4 patches applied: flag_submission()/override_hire_evaluation()
  # now auto-generate their audit-row id internally so the INSERT always
  # fires; ai_recommendation made a required param (was an unsafe None
  # default against a NOT NULL column); 2 new DB-row-level regression tests
  # added; AC2 wording corrected to match the shipped interface. 3 defer
  # items logged to deferred-work.md. 118/118 full suite green. Marked done.

  9-3-surface-guarded-mode-injection-failures: done
  # Implemented 2026-07-04: inject_workspace_files() now returns
  # {injected, guarded_mode_enforced} instead of silently returning None;
  # session_links gained ai_assistance_mode/guarded_mode_enforced columns;
  # generate_link() persists the REAL injection result; GET /api/submission
  # surfaces both fields. Backend/API only (AC8), no frontend badge.
  # Code review 2026-07-05: migration's `guarded_mode_enforced INTEGER
  # DEFAULT 1` backfilled pre-existing rows to 1 instead of NULL,
  # contradicting the documented (None, None) legacy-row contract — fixed
  # by dropping the DEFAULT clause; regression test added. API_REFERENCE.md
  # updated for the 2 new fields. 2 items deferred (injected-flag not
  # surfaced; candidates list doesn't expose guarded_mode_enforced).
  # 116/116 full suite green.

  9-4-session-expiry-during-open-modal: done
  # Implemented 2026-07-06: tickTimer() in app/routes/student.py now guards
  # against re-firing (let _expiryHandled) and, on the tick where
  # formatRemaining() first returns 'Expired', force-closes #nudgeModal and
  # shows one showToast(..., 'error', 8000) notice. Pure front-end change,
  # no backend/DB changes (story-creation research confirmed there's no
  # backend expiry enforcement to add to — expires_at is client-display-only).
  # Code review 2026-07-06 (3-layer): all 3 layers independently found the
  # timer's forced closeSubmitModal() fired even mid-fetch during an active
  # submission, violating AC5. Fixed via a _submitInFlight guard (set in
  # submitAssessment()'s try/finally) — submit modal now only force-closes
  # when no submission is in flight. 3 low-severity items deferred to
  # deferred-work.md. Verified: rendered HTML, node --check, full suite
  # 118/118 green. No JS test harness exists in this repo, matching the
  # established verification convention from prior JS-only stories.
  # Marked done.

  9-5-assignment-link-keyed-preview-fidelity: done
  # Implemented 2026-07-06: added GET /student/preview/assignment/<id> in
  # app/routes/student.py, reading the LIVE assignments row (via the existing
  # get_assignment()) instead of the challenges catalog template - fixes the
  # drift bug directly, no challenge_id disambiguation needed since the new
  # route is keyed on assignment_id. Extracted _render_student_preview_html()
  # so the ~230-line preview template isn't tripled; the existing challenge-
  # keyed route's behavior is proven byte-for-byte unchanged by 3 pinning
  # tests written before the refactor and passing unmodified after it. Added
  # a "Preview as Student" button to each card in the "Saved Assignments"
  # list (templates/frontend.html Tab 3), per the UI-placement decision.
  # Code review 2026-07-06 (3-layer): 1 patch (asymmetric test coverage for
  # the criteria-fallback path across the two routes, fixed with a mirrored
  # test), 1 defer (pre-existing rubric-JSON-vs-plain-text formatting
  # inconsistency from Story 6.4, logged to deferred-work.md). 125/125 full
  # suite green, live-verified via a real server. Marked done.

  9-6-verify-gemini-cli-auth-picker-manually: backlog
  # Not a code story - a one-time manual QA checklist item. Story 8.1's
  # security.auth.selectedType fix (verified live for headless `gemini -p`)
  # was never watched disappear in the real interactive picker inside an
  # actual browser terminal, because docker exec -t without a genuinely
  # attached terminal doesn't reliably drive Gemini CLI's ink-based UI.
  # Action: generate a candidate link, open the code-server terminal in a
  # real browser, type `gemini`, confirm no auth-method picker appears.
  # EXPANDED 2026-07-06 to also cover Story 9-7's live verification (no
  # Docker was available when 9-7 was implemented): in a guarded-mode
  # candidate link, confirm `rm ~/.gemini/GEMINI.md` / editing it fails,
  # and confirm `cd /tmp && gemini` still enforces the restriction.

  9-7-enforce-guarded-mode-immutable-context-file: done
  # SUPERSEDES the Story 6.5 code review's accepted honor-system decision
  # (2026-07-03) - user decided assessment integrity requires real
  # enforcement. Implemented 2026-07-06: GEMINI.md/settings.json move from
  # a post-start `docker cp` into /workspace (chmod 777, world-writable,
  # cwd-dependent) to a READ-ONLY bind mount established at container-
  # creation time (`docker run -v`), targeting Gemini CLI's GLOBAL context
  # path (~/.gemini, confirmed via Gemini CLI's own docs to always load
  # regardless of cwd) - immune to in-container root since the container
  # is never --privileged/no extra capabilities.
  # Code review 2026-07-06 (3-layer, extra scrutiny given security
  # sensitivity): found a critical gap - the original file-level mount
  # left ~/.gemini itself candidate-writable, letting a parent-directory
  # rename evict both mounts untouched. Fixed same-day: mount now targets
  # the WHOLE ~/.gemini directory (one -v flag) instead of two files -
  # the mount point itself is now the protected boundary. Also fixed a
  # cleanup prefix-boundary bug and bumped a log level. A SECOND, distinct
  # bypass was found and could NOT be closed by any mount fix: Gemini CLI
  # honors the $HOME env var, fully candidate-controlled via their shell -
  # accepted as a documented residual gap per user decision (same tier as
  # the already-accepted "external AI tool" bypass); real fix needs
  # network-level API validation, a future story. AC4 wording revised to
  # state precisely what a bind mount can/cannot guarantee.
  # create_container() now returns (container_id, port, guarded_mode_enforced);
  # inject_workspace_files() simplified back to instructions.md/solution.py
  # only; cleanup_container() removes the host-side directory via
  # `docker inspect` metadata, no separate tracking needed. 132/132 full
  # suite green. Residual gap: no Docker in this sandbox, so live
  # verification (does the mount actually block deletion/rename in a real
  # container, does Gemini CLI really load the global file) must happen
  # when Story 9-6 is done - folded into that story's scope. Marked done.

  epic-9-retrospective: optional
